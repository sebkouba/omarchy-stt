We've been working on our own keyboard shortcut implementation for long-running and push-to-talk dictations. 
There's currently a bug where when I use the push to talk shortcut, which is Control Alt Shift Super and E, for example. If I don't release all those keys simultaneously, the system immediately starts registering the other keys. So if I release the modifiers and hold E for a split second, it immediately starts typing E. Look at the lessons learned directories for what we've tried and come up with three different options for how we can avoid this. I'm thinking that as soon as one of the keys from the shortcut is released, all other currently pressed keys should also release. Does that make sense? 
